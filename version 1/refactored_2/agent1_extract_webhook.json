{
  "schema_version": "1.0",
  "agent_type": "specialized",
  "name": "[ES-CMP][R2] Extract Webhook Event Info",
  "output": {
    "type": "json",
    "schema": {
      "type": "object",
      "required": ["event_type", "request_id"],
      "properties": {
        "event_type": { "type": "string" },
        "request_id": { "type": "string" }
      }
    },
    "description": "event_type and request_id only; workflow routes on event_type equals 'status'."
  },
  "version": "1.0.0",
  "agent_id": "es-cmp-r2-extract-webhook",
  "file_urls": [],
  "is_active": true,
  "creativity": 0.1,
  "is_deleted": false,
  "parameters": [
    { "name": "Event Type", "type": "string", "default": null, "required": false, "description": "First element of webhook_payload.data.work_request.modified_fields (trimmed)." },
    { "name": "Request ID", "type": "string", "default": null, "required": false, "description": "webhook_payload.data.work_request.id (trimmed)." }
  ],
  "description": "Extract event_type and request_id from webhook. Output only these two fields. event_type = first element of modified_fields (trim); when status changed this is 'status'. No tools.",
  "enabled_tools": null,
  "agent_metadata": null,
  "inference_type": "simple",
  "prompt_template": "## Role / Goal\n\nYou are the Extract Webhook agent. Your goal is to output event_type and request_id so the workflow can route only when the change was a status change. Output only this JSON: { \"event_type\": ..., \"request_id\": ... }. Do not add other fields.\n\n## Guardrails (must follow)\n\n1. Output only event_type and request_id. Do not add any other fields.\n2. event_type = first element of webhook_payload.data.work_request.modified_fields, trimmed. When the changed field is the status field, this will be \"status\".\n3. request_id = webhook_payload.data.work_request.id, trimmed.\n4. Do not call any tools.\n\n## Input\n\n- webhook_payload.data.work_request.modified_fields (array); take first element, trim.\n- webhook_payload.data.work_request.id (string); trim.\n\n## Output\n\nReturn only this object:\n\n{\n  \"event_type\": <first element of modified_fields, trimmed>,\n  \"request_id\": <work_request.id, trimmed>\n}\n",
  "internal_version": 1,
  "is_enabled_in_chat": true
}
